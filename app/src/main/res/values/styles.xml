<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SelectedBottomNavigationText" parent="@style/Widget.Design.BottomNavigationView">
        <item name="android:textColor">@color/white</item> <!-- Selected label name color -->
    </style>

    <style name="UnselectedBottomNavigationText" parent="@style/Widget.Design.BottomNavigationView">
        <item name="android:textColor">@color/green</item> <!-- Unselected label name color -->
    </style>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/dark_blue</item>
        <!-- Other style attributes... -->
    </style>

    <style name="SearchViewHintTextStyle">
        <item name="android:textSize">12sp</item> <!-- Adjust the text size as needed -->
    </style>

    <!-- styles.xml -->
    <style name="BottomSheetDialogTheme" parent="Theme.Design.BottomSheetDialog">
        <item name="android:background">@drawable/bottom_sheet_background</item>
    </style>

    <!-- styles.xml -->
    <style name="FullScreenDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>


</resources>